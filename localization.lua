-----------------------------------------------------
-- localization.lua
-- English strings by default, localizations override with their own.
------------------------------------------------------

-- These don't need to be localized unless you want to mess with the format
FDA_STATUS_BASIC		= "%s - %s: %d/%d"		-- race, artifact, num fragments, num needed
FDA_STATUS_KEYSTONES	= "(+%dx %s: %d/%d)"	-- num keystones, keystone name, num fragments, num needed

FDA_CLICK_TO_SOLVE		= "Click to solve"
FDA_COST				= "Uses:"
FDA_SKILL_WASTE			= "(up to %d wasted)"
FDA_SKILLUP				= "Raises skill to:"
FDA_AUTO_USE_KEYSTONES	= "Automatically use\n%s"
FDA_GENERIC_KEYSTONES	= "%s keystones"
FDA_KEYSTONES_IN_BANK	= "(%dx %s in bank)"

------------------------------------------------------

if (GetLocale() == "frFR") then

end

------------------------------------------------------

if (GetLocale() == "deDE") then

end

------------------------------------------------------

if (GetLocale() == "esES" or GetLocale() == "esMX") then

end

------------------------------------------------------

if (GetLocale() == "ruRU") then

end

------------------------------------------------------

if (GetLocale() == "koKR") then

end

------------------------------------------------------

if (GetLocale() == "zhTW") then

end
